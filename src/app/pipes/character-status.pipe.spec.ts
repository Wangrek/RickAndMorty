import { CharacterStatusIconPipe } from './character-status.pipe';

describe('CharacterStatusIconPipe', () => {
  it('create an instance', () => {
    const pipe = new CharacterStatusIconPipe();
    expect(pipe).toBeTruthy();
  });
});
